# Security

Report security issues privately to `tasnim.2001040@bau.edu.bd`.

Do not attach patient data, controlled-access reads, credentials, access tokens, or identifiable
sample metadata. ViroFlow executes configured external tools locally; review configuration files
and reference assets from untrusted sources before running them.

